# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
SUPABASE_SECRET_KEY=your-service-role-key
SUPABASE_BOOKINGS_TABLE=bookings

# Admin page credentials. Use a strong, unique password in production.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-strong-password

# Private checklist URL password. This value is visible in the browser URL;
# it is a URL gate, not server-side authentication.
NUXT_PUBLIC_CHECKLISTS_PASSWORD=13515

# Optional local-only JSON path. Production must use Supabase.
# BOOKING_STORAGE_FILE=.data/bookings.json
